const REGION = "us-east-2";

const API_PATHS = {
  product: `https://.execute-api.${REGION}.amazonaws.com/dev`,
  order: `https://.execute-api.${REGION}.amazonaws.com/dev`,
  import: `https://.execute-api.${REGION}.amazonaws.com/dev`,
  bff: `https://.execute-api.${REGION}.amazonaws.com/dev`,
  cart: `https://.execute-api.${REGION}.amazonaws.com/dev`,
};

export default API_PATHS;
